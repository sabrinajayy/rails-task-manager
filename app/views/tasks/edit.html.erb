<div class="wrapper">
    <div class="container">
        <div class="row">
            <div class="col-xs-12 col-sm-6 col-sm-offset-3">
                <%= link_to "< Back to Tasks", '/tasks', class: 'btn' %>
                <%= link_to "Delete Task", task_path(@task), class: 'btn delete-btn',
                method: :delete,
                data: { confirm: "Are you sure?" } %>
                    <div class="content">
                        <h1>Edit a Task</h1>
                        <%= render 'form' %>
                    </div>
            </div>
        </div>
    </div>
</div>
